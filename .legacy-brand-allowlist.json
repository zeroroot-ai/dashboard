{
  "files": [
    {
      "path": "scripts/check-no-legacy-product-name.mjs",
      "reason": "the guard declares the four patterns it searches for"
    },
    {
      "path": ".legacy-brand-allowlist.json",
      "reason": "this allowlist quotes the very lines it allows"
    },
    {
      "path": "CHANGELOG.md",
      "reason": "release-please writes it, and the entry records the rename itself"
    }
  ],
  "lines": [
    {
      "path": "scripts/check-no-direct-daemon-grpc.mjs",
      "text": "// `gibson` package name or the `gibson.io` doc/comment string never",
      "reason": "names the string the daemon-host pattern must not match"
    },
    {
      "path": "src/lib/__tests__/brand.test.ts",
      "text": "expect(PRODUCT_NAME).not.toMatch(/Zero Day/);",
      "reason": "asserts the brand constant carries no trace of the old name"
    },
    {
      "path": "src/lib/__tests__/brand.test.ts",
      "text": "expect(PRODUCT_NAME_SHORT).not.toMatch(/Zero Day/);",
      "reason": "asserts the short brand constant carries no trace of the old name"
    }
  ]
}
